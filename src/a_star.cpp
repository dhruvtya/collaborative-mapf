#include "a_star.hpp"

void AStar::buildConstraintTable(){
    // TODO
}

void AStar::findAStarPath(){
    // Input: map, starts, goals, heuristics, agent ID, constraints
    // Output: path, a list of movable objects in the path (empty if it is a clean path)
    // It is preferable if only path is returned and the list of movable objects is passed as reference in arguments, store in place

    // TODO
}